## synk

> Keep your documentation in sync with your code, automatically.

`synk` is an AI-powered documentation assistant that reviews your code changes and suggests precise documentation updates. Stop letting your docs drift — let `synk` handle the tedious work of keeping documentation accurate.