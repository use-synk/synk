{
	"extends": ["//"],
	"tasks": {
		"build": {
			"outputs": [".next/**", "!.next/cache/**"]
		},
		"typecheck": {
			"dependsOn": ["^build", "^typecheck"],
			"outputs": ["tsconfig.tsbuildinfo", ".next/cache/.tsbuildinfo"]
		}
	}
}
