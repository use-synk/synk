# Auth-specific configuration — shared vars are loaded from root .env via --env-file=../../.env.
# Only define values here that are specific to this package or override the root .env.

# URL of the auth server (used for callback URLs and token issuance)
# BETTER_AUTH_URL=http://localhost:3030
