{
	"name": "@synk-ai/auth",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"files": ["dist"],
	"exports": {
		"./server": {
			"types": "./src/server.ts",
			"default": "./dist/server.js"
		},
		"./client": {
			"types": "./src/client.ts",
			"default": "./dist/client.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"generate": "bun x auth@latest generate --config src/cli.ts --output ../db/src/schemas/auth.prisma",
		"lint": "biome check .",
		"lint:write": "biome check . --write",
		"lint:unsafe": "biome check . --write --unsafe",
		"typecheck": "bunx tsc --noEmit",
		"test": "bun --env-file=../../.env --env-file=.env test src --coverage"
	},
	"dependencies": {
		"@better-auth/prisma-adapter": "^1.5.1",
		"@synk-ai/db": "workspace:*",
		"better-auth": "^1.5.1"
	},
	"devDependencies": {
		"typescript": "5.9.2"
	}
}
