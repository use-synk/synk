# Package-specific overrides — shared vars are loaded from root .env via --env-file=../../.env.
# Only define values here if they differ from the root .env for this package.

# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/synk_ai?schema=public
