{
	"extends": ["//"],
	"tasks": {
		"generate": {
			"outputs": ["src/generated/**"]
		},
		"build": {
			"dependsOn": ["generate", "^build"],
			"outputs": ["dist/**"]
		},
		"typecheck": {
			"dependsOn": ["generate", "^typecheck"],
			"outputs": []
		}
	}
}
